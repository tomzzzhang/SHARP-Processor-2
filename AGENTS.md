# AGENTS.md — SHARP Processor 2

**Last Updated:** 2026-07-26 PST — Claude (Mac session — `sharpplot` headless figure CLI MERGED to `main`, branch branch `feature/sharpplot-cli`; see `CLAUDE.md` #58 and `docs/SHARPPLOT.md`. Nothing merged, nothing released. **eslint baseline is 48 (22 errors, 26 warnings)**, measured on `e8356b5` — the "50" quoted below was stale. New gate: `npm run test:parity` must report 4/4 identical before any commit that touches `plot-figure.ts` or what it depends on. Prior — 2026-07-06 Mac session — **v0.2.3 shipped (macOS side)**: Kinetics Report residuals + melt-Tm hover/click + data/fit toggles (#56); committed `32f5faf`, tagged `v0.2.3`, macOS DMG published to the `v0.2.3` pre-release (`v0.2.2` stays "Latest", Windows installers queued); this doc unchanged. **Prior — v0.2.2 macOS DMG published to the v0.2.2 GitHub release; v0.2.2 is "Latest", tag frozen at `0ee94fc`.** **Prior — macOS sign-off + release** — #51 live-UI changes verified on WKWebView (red checkmark checkbox toggles; gesture hint top-right beside the house = sole modebar button; User Manual greyscale mockup); both Mac DMGs built, verified, and **published to GitHub releases** — `SHARP.Processor.2_0.1.13_aarch64.dmg` → v0.1.13 (first-ever Mac build, was Windows-only) and `..._0.2.0_aarch64.dmg` → v0.2.0 pre-release. **Merged to `main`** via PR #10 (`4c10627`). Prior, on branch `feature/ui-ux-enhancements`: **Live-UI tweaks (CLAUDE.md #51)** — (1) all binary **toggles now render as a native checkbox** (red checkmark box) instead of #49's radio-dot (the tri-state per-well auto-baseline stays an `indeterminate` checkbox; real radio groups — baseline Method, X-axis unit — untouched); (2) the **plot gesture-hint moved to the top-right beside the reset/"house" button** at its natural ~house-icon size on a theme-aware translucent chip — `displaylogo:false` makes the house the sole rightmost modebar control, and #50's `extraBottom` bottom-strip reservation was reverted (resolving the original hint↔x-label collision); (3) the in-app **User Manual** mockup updated to match and greyscaled per request. `tsc -b` + `vite build` clean, eslint 50=50 (zero new). **No behaviour change** — presentation only. Committed + pushed (`f3e0f4b`); Windows x64 + x86 0.2.0 installers rebuilt (`dist-release/`); Mac builds prepared via OneDrive `MAC_BUILD_HANDOFF.md`. Live-UI sign-off **PASSED on macOS**; both DMGs published (see top of this entry). Prior: #48 restyle, #49 dialogs/notes + User Manual fact-check, #50 axis-label scaling, #47 v0.2.0 beta post-release fixes.)

The shared project instructions live in [`CLAUDE.md`](CLAUDE.md).

Codex should follow `CLAUDE.md` as the source of truth for project architecture,
build/run instructions, file-format notes, and collaboration rules. Machine-local
details live in `CLAUDE.local.md`, and cross-agent coordination notes live in the
shared OneDrive Markdown folder documented there.
